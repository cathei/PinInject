using System;
using System.Collections;
using System.Collections.Generic;

namespace Cathei.PinInject
{
    public static class InjectContainerPool
    {
        // private static readonly Stack<InjectContainer> _pool = new Stack<InjectContainer>();

        // internal static InjectContainer Borrow()
        // {
        //     if (_pool.Count > 0)
        //         return _pool.Pop();
        //     return new InjectContainer();
        // }

        // internal static void Return(InjectContainer container)
        // {
        //     container.Reset();
        //     _pool.Push(container);
        // }
    }
}